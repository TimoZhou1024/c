#include <stdio.h>
int main(){
    char a[1024];
    char b[1024];
    char c[1024];
    fgets(a,1024,stdin);
    fgets(b,1024,stdin);
    /*
    int strmax,strmin;
    strmax=strlen(a)>strlen(b)?strlen(a):strlen(b);
    strmin=strlen(a)<=strlen(b)?strlen(a):strlen(b);
    */
    for(i=0,carry=0;i<a[0])

    

    return 0;
}